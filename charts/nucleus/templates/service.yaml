apiVersion: v1
kind: Service
metadata:
  name: {{ include "soma-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "soma-app.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "soma-app.selectorLabels" . | nindent 4 }}
